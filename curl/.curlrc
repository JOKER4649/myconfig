# 自動讀取 ~/.netrc 取得認證資訊（chmod 600）
netrc

# 偏好 markdown（Cloudflare Markdown for Agents、IETF draft），
# 與 Claude Code / OpenCode 格式一致；*/* 結尾避免 406 風險。
# 需覆寫時用 -H "Accept: ..."，例如 MCP Streamable HTTP。
header = "Accept: text/markdown, text/html, */*"

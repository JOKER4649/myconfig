*.log
*.pyc

# agent
.omo/
.agent/

# herdr 安裝的整合檔（自動生成，不應手動追蹤）
opencode/plugins/herdr-*.js

# 手動歸檔的非 git 檔案（mv 進來待清理）
.delete/

.env

# Hindsight stack runtime state — re-fetched on `docker compose up`
# 加 leading `/` 鎖定 repo root；trailing `/` 只 match 目錄
# service-local .gitignore 也放在 services/hindsight/.gitignore (備援)
/services/hindsight/tei_data/
/services/hindsight/pg_data/
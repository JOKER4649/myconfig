## User preference
- 語言: 程式碼**外**使用`繁體中文`, 程式碼**內**尊重專案慣例(預設簡體中文)
- 程式碼: 可讀性 > 最小變更 > 可靠性 > 性能
  - 當既有 package 可以解決問題, 就不要造輪子, 哪怕會增加 dist 大小
  - 當通過刪減同樣可以達成目的, 就不要增加, `減法` 優於 `加法`
- 如果資料庫可以解決的事情, 就不要引入其他外部功能/概念
